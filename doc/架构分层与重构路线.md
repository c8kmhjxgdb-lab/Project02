# 星愿之子架构分层与重构路线

本文记录当前项目的推荐分层、核心原则、依赖方向和渐进式重构路线，作为后续重构与新增系统时的工程约束。

## 当前评价

项目已经具备清晰的原型基础：`src/Engine/` 承载渲染、物理、摄像机、脚本等基础设施，`src/Game/` 按玩法域拆分了世界、能力、AI、社交、情绪、建造、库存、任务、玩具和存档等模块。

主要维护风险集中在 `src/main.cpp`：入口、资源初始化、输入、主菜单、玩法规则、存档套用、物理更新、渲染排序和 UI 绘制集中在同一个文件中。这个结构适合阶段式快速验证，但继续扩张会导致回归风险高、初始化顺序脆弱、跨系统改动难以测试。

## 推荐分层

```text
src/
├── Platform/              # SDL/OpenGL窗口、输入设备、时间、文件系统
├── Core/                  # Application、GameLoop、SceneManager、EventBus、服务注册
├── Engine/
│   ├── Renderer/          # Draw2D、TextRenderer、RenderQueue、后处理、SDF渲染
│   ├── Physics/           # Box2D封装、碰撞查询、物理句柄
│   ├── Audio/             # 音频设备、BGM、SFX、混音与淡入淡出
│   ├── Camera/            # 2D摄像机
│   ├── Scripting/         # LuaVM、脚本绑定、脚本配置加载
│   └── Resource/          # Shader、Texture、Font、Script、Audio资源管理
├── Game/
│   ├── Domain/            # 纯数据与实体：Player、Enemy、NPC、Quest、Region、Item
│   ├── Systems/           # Combat、Ability、AI、Quest、Emotion、Weather、Time、Building
│   ├── Controllers/       # PlayerController、MenuController、InteractionController
│   ├── Presentation/      # HUD、DialogueView、MiniMapView、WorldRenderer、MenuView
│   ├── Services/          # SaveService、RegionService、DialogueService、AudioService
│   └── Data/              # SaveRepository、LuaConfigRepository、JsonSerializer
├── App/                   # 组合根：创建服务、装配场景、启动游戏
└── Utils/                 # 数学、随机、字符串、通用小工具
```

现阶段不要求一次性移动到完整目录结构。先在现有目录下按职责抽类，等边界稳定后再做目录迁移。

## 层职责

- 启动层：只负责初始化运行环境、创建 `Application`、进入 `run()`、退出清理。
- 循环层：固定事件、输入、更新、碰撞、清理、渲染、交换缓冲的顺序。
- 平台层：隔离 SDL、窗口、OpenGL 上下文、文件路径和系统时间。
- 引擎层：提供可复用能力，不感知具体游戏概念。
- 游戏实体层：保存游戏世界中的领域数据，避免直接持有渲染或平台资源。
- 游戏系统层：处理时间推进、战斗、AI、任务、天气、情绪等规则。
- 控制层：把输入和 UI 命令翻译为游戏意图。
- 服务层：组织跨系统用例，例如保存、读档、区域切换、对话启动、音频策略。
- 接口层：定义服务、资源、存储和平台抽象，减少具体实现泄漏。
- 数据访问层：负责 JSON、Lua、文件读写和版本迁移。
- 视图层：只读取状态并绘制画面，不承载玩法规则。
- 工具层：只放无状态或低耦合的通用工具。

## 核心原则

1. `main.cpp` 只做启动层  
   目标是把 `main.cpp` 缩到几十行：初始化 `Application`，调用 `run()`，处理返回码。

2. 主循环顺序显式化  
   当前物理、碰撞清理、渲染和存读档顺序很关键，必须沉淀为 `GameLoop` 或 `GameScene` 的固定流程：

   ```text
   poll events
   scene.handleInput
   fixedUpdate physics
   gameplay systems update
   collision resolution
   cleanup
   camera/minimap lateUpdate
   render world
   render UI
   swap buffers
   ```

3. 避免继续扩大 `GameState`  
   先将 `GameState` 拆为更小上下文，再逐步转入场景和服务：

   ```text
   WorldState      # 区域、地图、装饰、时间、天气
   PlayerState     # 血量、魔法、位置、童心、飞行、技能冷却
   CombatState     # 敌人、弹幕、掉落、粒子
   UIState         # 菜单、提示、对话、HUD
   RuntimeServices # renderer、physics、audio、scripting、resources
   ```

4. 玩法状态与视图状态分离  
   渲染器和 UI View 接收只读 ViewModel，不直接依赖完整 `GameState`。

5. 服务层承接跨系统流程  
   保存/读取、区域切换、对话启动、音频播放等跨多个系统的流程，不应散落在主循环。

6. 数据驱动要收口  
   Lua 配置、JSON 存档和资源路径由 Repository 或 Resource 层管理，玩法系统不直接拼路径。

7. 引擎不反向依赖游戏  
   `Engine` 可以暴露能力和接口，但不能知道 `Princess`、`Quest`、`WeatherSystem` 等具体玩法概念。

8. 渐进式重构优先  
   每次只抽一个明确边界，保持可编译、可运行，避免一次性搬动主循环导致行为回归。

## 依赖方向

理想依赖方向如下：

```text
App
 ↓
Core / Scene
 ↓
Game Controllers / Services / Presentation
 ↓
Game Systems
 ↓
Game Domain
 ↓
Engine Interfaces
 ↓
Platform / Third-party
```

约束：

- `Engine` 不依赖 `Game`。
- `Game/Domain` 不直接调用 SDL、OpenGL、Box2D 文件 API。
- `Presentation` 可以读状态并调用渲染接口，但不能修改玩法规则。
- `Services` 可以协调多个系统，但不直接绘制。
- `Data` 负责持久化格式，不负责解释玩法规则。
- 第三方类型要尽量限制在边界内，例如 Box2D 的 `b2BodyId` 后续应逐步被物理句柄或适配层包住。

## 推荐场景结构

```cpp
class IScene {
public:
    virtual ~IScene() = default;
    virtual void enter() = 0;
    virtual void handleEvent(const InputEvent& event) = 0;
    virtual void update(float dt) = 0;
    virtual void render(RenderContext& ctx) = 0;
    virtual void exit() = 0;
};
```

短期目标场景：

- `MainMenuScene`：主菜单输入、菜单视图、开始/读取/退出命令。
- `WorldScene`：当前游戏世界更新和渲染。
- `LoadingScene`：后续资源加载或区域切换过渡。
- `PauseScene`：后续暂停、设置、读档。

## 渐进式重构路线

### 阶段 1：降低 `main.cpp` 密度

- 抽出 `MainMenuView`，移走菜单绘制和点击命中计算。
- 抽出 `HudView`，移走 HUD 绘制。
- 抽出 `WorldRenderer`，移走 TileMap、装饰、角色、敌人、投射物、粒子渲染。
- 抽出 `InputState`，替代主循环内直接操作 SDL 键盘数组。

### 阶段 2：建立场景边界

- 引入 `IScene`、`SceneManager`。
- 将 `AppMode::MainMenu / Playing` 迁移为 `MainMenuScene / WorldScene`。
- 主循环只负责事件分发、更新时间、调用当前场景。

### 阶段 3：拆分运行时上下文

- 将 `GameState` 拆成 `WorldState`、`PlayerState`、`CombatState`、`UIState`、`RuntimeServices`。
- 系统只接收自己需要的上下文。
- 避免新增功能继续向 `GameState` 追加字段。

### 阶段 4：服务层重整

- 将 `saveCurrentGame`、`loadGameSlot`、`applySaveData` 迁移到 `SaveService`。
- 将区域切换和天气/小地图刷新迁移到 `RegionService`。
- 将对话启动、选择、奖励派发迁移到 `DialogueService`。

### 阶段 5：数据访问层重整

- 拆分 `SaveSystem` 为 `SaveService`、`SaveRepository`、`SaveSerializer`、`SaveSnapshotBuilder`、`SaveApplier`。
- 将 Lua 配置读取收口到 `LuaConfigRepository`。
- 为存档版本迁移保留 `SaveMigration`。

### 阶段 6：补齐音频层

- 新增 `Engine/Audio`。
- 接入 SDL_mixer、SoLoud 或 MiniAudio 之一。
- 先实现区域 BGM、技能 SFX、UI SFX。
- 后续通过事件驱动播放：`RegionEntered`、`SkillCast`、`EnemyKilled`、`WeatherChanged`。

### 阶段 7：CMake 模块化

- 将当前单一 `Starchild2D` target 拆成：

  ```text
  StarchildEngine
  StarchildGame
  StarchildApp
  ```

- `StarchildApp` 只链接并装配前两者。
- 为纯逻辑系统预留测试 target。

## 首轮重构验收标准

- `main.cpp` 行数下降，至少移除一个完整 UI 或渲染职责。
- 菜单、游戏启动、读档、退出行为保持不变。
- CMake Release 构建通过。
- 不修改 `build/` 生成物。
- 每一步重构都保持项目处于可编译状态。

## 代码现状快照（2026-06-10）

本节记录按阶段 1 到阶段 7 对当前代码的对照结果，供后续继续重构时判断已完成边界和剩余风险。当前仓库已经完成入口瘦身、最小场景边界、服务层雏形、音频边界和 CMake target 模块化；后续路线 3 的重点不再是继续横向新增边界，而是纵向收窄 `GameState`、Presenter、输入、存档套用和测试覆盖。

### 阶段 1 现状：已完成，进入后续深化

已完成：

- `src/main.cpp` 已缩到创建 `Application` 并调用 `run()`，入口层目标基本达成。
- 菜单绘制与点击命中已抽到 `Game/Presentation/MainMenuView`。
- HUD 已抽到 `Game/Presentation/HudView`，并通过 ViewModel 渲染。
- TileMap、装饰等世界渲染已抽到 `Game/Presentation/WorldRenderer`，角色、敌人、投射物、粒子也拆到多个 Presentation 模块。
- 主渲染编排集中到 `Game/Presentation/GameRenderer`，不再堆在入口文件中。
- `GameRenderer` 已新增 `MainMenuRenderContext` 和 `WorldRenderContext`，主菜单/世界场景渲染入口已改为显式渲染上下文调用。
- `GameRenderer` 中完整 `GameState&` 渲染入口和 `make*RenderContext(GameState&)` 兼容适配已删除，主菜单渲染上下文由 `MainMenuScene` 组装，世界渲染上下文已迁到 `Game/Presentation/WorldPresentationBuilder`。
- 已抽出 `Game/Controllers/InputState`，键盘和鼠标运行时输入状态不再以裸字段散落在 `GameState` 中。
- 移动和玩具小游戏等系统已通过 `InputState::isDown()` 读取输入，不再直接读取 `GameState::keys[SDL_NUM_SCANCODES]`。

后续深化风险：

- `WorldPresentationBuilder` 仍从完整 `GameState` 组装 `WorldRenderContext`，视图状态与玩法状态还没有完全拆成独立 Presenter/ViewModel 管线。

### 阶段 2 现状：已完成最小多态场景边界

已完成：

- 已存在 `Game/Scenes/SceneManager`、`MainMenuScene`、`WorldScene`。
- 主循环已变成事件轮询后调用当前场景更新与渲染。
- `MainMenu / Playing` 的更新渲染路径已分别进入 `MainMenuScene` 和 `WorldScene`。
- 已新增 `IScene` 多态接口，包含 `enter / handleEvent / update / render / exit` 生命周期。
- `SceneManager` 已开始持有场景对象，并通过当前场景分发事件、更新和渲染。
- `AppMode` 已从 `GameState.h` 拆到 `Game/Scenes/AppMode.h`。
- 外部新游戏和读档流程已改为返回进入 Playing 的意图，由 `GameLoop` 调用 `SceneManager::requestMode()` 完成切换。
- 模式状态已从 `GameState` 移到 `SceneManager::State`，`GameState` 不再保存 `appMode`。
- 主菜单输入已由 `MainMenuScene` 分发到 `MainMenuInputController`，游玩输入由 `WorldScene` 分发到通用 `InputController`，输入控制器不再通过 `GameState::appMode` 判断场景。
- 新游戏和读档流程只返回“进入游玩”意图，实际场景切换由 `GameLoop` 驱动 `SceneManager` 完成。
- `MainMenuScene`、`WorldScene` 已提供 `create()` 工厂并返回真正的 `IScene` 实例；`WorldScene` 的标题、更新和 tile color 状态已由场景对象内部持有，`SceneManager` 不再维护 `WorldScene::State` 适配器。

后续深化风险：

- `MainMenuScene`、`WorldScene` 仍保留命名空间函数入口作为兼容层，场景方法仍接收完整 `GameState&`，还没有真正拥有更窄依赖。
- 输入分发已经按主菜单/世界场景分开，但世界内实际输入规则仍主要由全局 `InputController` 和各 Controller 完成，尚未继续按玩法域拆小上下文。

### 阶段 3 现状：进行中，已建立最小上下文骨架

已完成或部分完成：

- 已新增 `Game/StateContexts.h/.cpp`，提供 `WorldState`、`PlayerState`、`CombatState`、`UIState`、`RuntimeServices` 的最小上下文视图。
- 当前上下文以引用方式包住既有 `GameState` 字段，后续系统可以逐步从接收完整 `GameState&` 改为接收自己需要的小上下文。
- 已新增 `Game/State/GameUiState.h`，菜单选择、菜单消息、阶段 7 通知和公主基地对话标记等运行时 UI 字段已从 `GameState` 顶层迁入 `GameState::ui`。
- `NoticeService`、主菜单输入、菜单渲染模型、读档提示、玩家生命周期通知计时和任务进度对话标记已改为读写 `GameState::ui`。
- `RegionUpdateService` 已完成第一条低风险调用链收窄：新增 `RegionUpdateService::Context` 和 `makeContext(GameState&)` 过渡适配器，核心 `update()` 不再接收完整 `GameState&`。
- 区域切换后的天气/建造/临时战斗刷新已迁到 `RegionService::GameplayContext`，旧区域刷新兼容门面已清理。
- `PlayerMotionService` 已完成第二条低风险调用链收窄：新增 `PlayerMotionService::Context` 和 `makeContext(GameState&)`，移动、飞行、地形/天气修正、地形伤害、物理步进和相机跟随已改为显式依赖。
- `WorldNpcUiUpdateService` 已完成第三条窄依赖调用链收窄：新增 `WorldNpcUiUpdateService::Context` 和 `makeContext(GameState&)`，宣泄动画、对话 UI 和公主跟随更新不再直接接收完整 `GameState&`。
- `WorldCombatUpdateService` 已完成第四条主路径调用链收窄：新增 `WorldCombatUpdateService::Context` 和 `makeContext(GameState&)`，并补齐 `ProjectileTrailService::Context`、`CombatCollisionService::Context`、`CombatService::handleCollisions(Context&)` 适配入口，避免战斗更新通过拖尾和碰撞逻辑回退到完整 `GameState&`。
- `EnemySpawnService` 已完成第五条窄依赖调用链收窄：新增 `EnemySpawnService::Context` 和 `makeContext(GameState&)`，并补齐 `CombatService::SpawnContext` 适配入口，刷怪更新不再直接接收完整 `GameState&`。
- `ProgressionUpdateService` 已完成第六条高耦合调用链收窄：新增 `ProgressionUpdateService::Context` 和 `makeContext(GameState&)`，并把情绪/家园恢复、玩具奖励、任务快照、任务奖励拆成局部步骤，通过回调注入提示能力。
- `PlayerLifecycleService` 与 `AbilityUpdateService` 已完成上下文收窄，`WorldUpdateService` 主更新路径中的生命周期、技能、移动、战斗、区域、成长、NPC/UI、刷怪更新都已改为显式 `Context` 调用。
- `AbilityInputController` 已完成输入链路首轮收窄：新增 `AbilityInputController::Context` 和 `Callbacks`，飞行、护盾、抓举、羁绊技等直接输入处理已改为显式依赖，`InputController` 主路径不再把完整 `GameState&` 透传给能力输入控制器。
- `InteractionInputController` 已完成交互链路首轮收窄：新增 `InteractionInputController::Context` 和 `Callbacks`，对话推进、休息、宣泄、公主互动等核心输入处理已改为显式依赖，`InputController` 主路径不再把完整 `GameState&` 透传给交互输入控制器。
- `CombatService` 的玩家施法入口已完成上下文收窄：新增 `CombatService::CastContext`，火球、冰刺和闪电施法核心逻辑不再直接接收完整 `GameState&`，`AbilityInputController` 也不再通过回调捕获完整 `GameState&` 触发施法。
- `CombatService`、`CombatCollisionService`、`ProjectileTrailService` 和 `EnemySpawnService` 中未使用的 `GameState&` 兼容包装函数已删除，调用方保留显式上下文入口。
- `BuildingInputController` 已完成建造输入链路首轮收窄：新增 `BuildingInputController::Context` 和 `Callbacks`，建造开关、家具移动/删除、购买/摆放、玩具小游戏启动和滚轮选择已改为显式依赖，`InputController` 主路径不再把完整 `GameState&` 透传给建造输入控制器。

未完成或未完全完成：

- `GameState` 仍是事实上的聚合根，但 UI 运行时字段已开始迁入独立子对象；世界、玩家、战斗等字段尚未真正迁移到独立拥有状态对象。
- `WorldUpdateService` 主更新路径和输入控制主路径已基本完成首轮上下文化，但读档/新游戏流程等调用链仍大量直接使用完整 `GameState&`。
- 交互输入中的进出家门已通过回调进入 `RegionService`，提示展示已通过回调进入 `NoticeService`。
- 建造输入中的鼠标世界坐标查询仍通过回调复用 `PlayerInputQuery`，提示展示已通过回调进入 `NoticeService`；后续可继续收窄到平台输入视图和 UI 提示上下文。
- `RuntimeServices` 还只是最小视图，渲染和资源边界仍可继续系统化；音频边界已建立并开始资源清单化。

### 阶段 4/5 现状：进行中，保存与数据访问已部分提前推进

已完成或部分完成：

- 保存流程已拆出 `SaveGameService`、`SaveSnapshotBuilder`、`SaveApplier`。
- 已新增 `RegionService`，承接区域玩法上下文刷新、天气区域上下文刷新、临时战斗清理和家门区域切换；`RegionUpdateService`、启动初始化、新游戏、读档和交互输入已开始直接调用 `RegionService`。
- 已新增 `DialogueService`，承接对话导航/推进、基地床铺休息、基地/野外公主对话、情绪奖励、公主好感和宣泄流程；`InteractionInputController` 已基本退化为输入意图分发。
- 已新增 `NoticeService`，承接阶段 7 通知文本和计时器写入；原 `SessionService::showNotice(GameState&)` 兼容门面已移除。
- 存档重置流程已直接使用 `RegionService::GameplayContext` 清理临时战斗状态，原 `SessionService::clearTransientCombat(GameState&)` 兼容门面已移除。
- `SessionService.h/.cpp` 已删除，区域、通知和会话重置职责不再集中在同一个过渡服务中。
- 文件读写和 JSON 序列化已拆到 `Game/Data/SaveRepository`、`SaveSerializer`。
- `SaveSystem` 门面已从 `GameState` 移除，旧 `SaveSystem.h/.cpp` 已删除；`SaveGameService` 直接组合 `SaveRepository`、`SaveSerializer`、`SaveMigration` 和 `SaveApplier`。
- 已新增 `SaveMigration`，为存档版本迁移保留统一入口。
- 已新增 `LuaConfigRepository`，集中管理主要 Lua 配置路径和共享运行时脚本加载，`GameBootstrap` 与新游戏重置不再直接拼主要配置路径。
- Lua 配置读取已有多个 Data Loader，例如天气、时间、情绪、家具、玩具、任务、对话脚本 Loader。
- 读档前已重建 `RegionManager` 的当前区、缓存区和发现区，避免旧会话访问过的区域污染旧存档。
- 旧任务存档 fallback 已改为先重建 `questEntries`，再只把存档明确列出的任务标记为 `Rewarded`。

未完成或未完全完成：

- `NoticeService` 已建立，但仍保留 `makeContext(GameState&)` 过渡适配器；后续可以由场景或 Presenter 直接传入 UI 通知上下文。
- `DialogueService` 已建立，提示展示已通过回调进入 `NoticeService`，但通知 UI 状态仍在 `GameState` 中，后续可继续迁到独立 `UIState`。
- `LuaConfigRepository` 目前主要收口路径和共享脚本加载，具体解析仍由各系统 `loadConfig/loadDefinitions` 和既有 Loader 完成。

### 阶段 6 现状：已完成最小音频边界，资源管线待后续素材驱动

已完成或部分完成：

- 已新增 `Engine/Audio/AudioSystem`，作为当前无外部依赖的音频设备抽象，占位承接 BGM/SFX 播放请求。
- 已新增 `Game/Services/AudioService`，提供区域 BGM、技能 SFX、UI SFX 的语义入口。
- `GameState` 已持有 `AudioSystem`，启动和关闭流程已初始化/释放音频系统。
- `RegionService` 区域玩法刷新时会触发区域 BGM，`CombatService` 施法时会触发技能 SFX。
- `AudioSystem` 已接入 SDL2 音频设备，当前用程序化音色生成最小 BGM/SFX，后续可在同一边界替换成资源驱动混音。
- 已新增 `assets/audio/manifest.json`，音频 ID、可选 WAV 路径、fallback 频率、时长、音量和循环策略开始由资源清单描述。
- `AudioSystem` 已支持加载音频清单、可选 WAV clip、主音量/BGM/SFX 音量、BGM 淡入淡出和资源缺失时的程序化 fallback。
- 已新增 `AudioManifestTest`，覆盖音频清单加载、资源 ID 注册、音量钳制和无音频设备状态下的播放状态切换。

后续深化风险：

- `AudioSystem` 已有真实 SDL 音频输出和 WAV 资源清单边界，但项目目前还没有真实音频素材；OGG/MP3、流式 BGM、音频缓存淘汰和更完整的混音策略仍待后续接入。
- UI SFX 入口已存在，但主菜单、按钮、对话确认等 UI 事件尚未系统接入。

### 阶段 7 现状：已完成 CMake target 模块化

已完成：

- `CMakeLists.txt` 已从单一 `Starchild2D` target 拆成 `StarchildEngine`、`StarchildGame`、`StarchildApp` 和最终可执行文件 `Starchild2D`。
- 当前依赖方向为 `Starchild2D -> StarchildApp -> StarchildGame -> StarchildEngine`，与推荐分层方向一致。
- 源码尚未大规模搬迁目录，先通过 target 边界建立模块化编译结构，降低行为回归风险。
- 已启用 `CTest`，并新增 `SaveDataRoundTripTest`，为纯逻辑数据访问层提供最小测试 target。
- 已新增 `CombatCastServiceTest`，覆盖玩家投射物施法成功、冷却阻止、死亡阻止和零向量瞄准 fallback，验证战斗施法服务可以通过显式上下文进行纯逻辑测试。
- 已新增 `QuestSystemFlowTest`，覆盖任务不在基地不推进、满足条件只奖励一次、任务存档恢复和目标进度追踪。
- 已新增 `RegionServiceDoorTest`，覆盖新手村/秘密基地门交互、区域上下文刷新、玩家传送和进入基地时的临时战斗状态清理。
- 已新增 `DialogueServiceRewardTest`，覆盖基地床铺休息、雨夜谈心、低童心安抚和宣泄的奖励/提示副作用。
- 已新增 `tests/TestSupport.h`，替代 Release 配置下会失效的 `assert`，确保 CTest 在 Release 构建中也执行真实断言。

后续深化风险：

- 目前已有数据访问、战斗施法、任务流程、区域门交互和对话奖励的首轮测试；后续仍需覆盖读档套用完整流程、碰撞结算、NPC 行为和输入控制边界。
- target 拆分仍是源码列表级别，后续可以继续把资源、平台和第三方边界进一步细化。

### 当前下一步优先级

1. 路线 3 第三批：继续扩展测试覆盖，优先补 NPC 行为、区域缓存和更多输入控制边界测试。
2. 路线 3 第四批：继续收窄输入与 UI 通知边界，减少 `NoticeService::makeContext(GameState&)`、`PlayerInputQuery(GameState&)` 等过渡适配入口。
3. 路线 3 第五批：在测试覆盖增长后，再推进真实状态子对象拆分；优先玩家、战斗、世界状态，避免无保护地搬迁 `GameState` 字段。
4. 路线 3 第六批：把 `MainMenuScene`、`WorldScene` 从命名空间函数和适配器推进到真正拥有依赖与局部状态的场景类。
5. 路线 3 第七批：音频层保留现有清单和 fallback 策略，等真实素材进入仓库后再接 OGG/MP3、流式 BGM、缓存淘汰和更完整混音策略。

### 路线 3 执行记录

#### 2026-06-10 第一批

- 已新增 `Game/Presentation/WorldPresentationBuilder`，把 `WorldScene` 中的 `WorldRenderContext` 组装迁到 Presentation 层；场景层只负责调用 builder、渲染和窗口标题更新。
- 已新增 `SaveApplierIntegrationTest`，覆盖读档套用后的玩家位置、生命、魔法、金币、时间、天气、情绪、区域、玩具、任务、公主状态、通知文本和临时战斗/UI 状态清理。
- 已把 `AudioService::playUiSfx()` 接入主菜单导航/确认/取消、游玩态取消、保存和读档输入路径，并为 `sfx/ui/navigate` 补齐音频清单 fallback。

#### 2026-06-10 第二批

- 已新增 `CombatCollisionServiceTest`，覆盖玩家投射物命中敌人、owner body 防误伤和死亡玩家忽略敌人接触伤害。
- 已新增 `InputControllerBoundaryTest`，覆盖能力输入在死亡、建造模式和玩具小游戏状态下的边界，以及建造模式切换和滚轮消费行为。
- `AbilityInputController::handleMouseButtonDown()` 已补齐与键盘能力输入一致的玩法状态检查，避免绕过上层输入分发时在建造模式或小游戏中施法。

#### 2026-06-10 第三批

- `MainMenuScene` 和 `WorldScene` 已新增 `create()` 工厂，直接返回 `IScene` 实例。
- `WorldScene` 的 `WindowTitlePresenter::State`、`WorldUpdateService::State` 和 `TileColors` 已由场景对象内部持有，`SceneManager` 不再通过本地 adapter 和外部 `WorldScene::State` 指针管理世界场景状态。
- `SceneManager` 现在只负责场景对象选择、生命周期切换和事件/更新/渲染分发，具体场景适配细节已回到各自 Scene 文件。
